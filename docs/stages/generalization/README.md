# Abstraction, boundary, and Qwen generalization stages

`abstraction_discovery_v1` asks whether a downstream abstraction is uniquely identified. E is the strongest descriptive candidate, but the full identity gate does not pass. `ood_free_generation_v1` is a frozen boundary experiment and is not evidence that E identity was established.

`qwen_fixed_setting_stability_v1` and `qwen_fresh_contexts_v1` are supporting extensions. `qwen_confirmation_v2` is the claim-bearing independent extension: natural-effect recovery is primary and cognitive-target recovery is a separate secondary endpoint.

Replay C04–C09 with `python -m cognitive_discovery.reproduce claim C07` (replace the claim ID as needed). The pinned fixed-setting experiment is the only current GPU rerun admitted by the guarded runner; other extensions remain compact-replayable because required raw inputs are absent. See [CLAIMS.md](../../../CLAIMS.md) for exact limitations.
