# Causal mechanistic stages

`mechanistic_manifest_v1` is the live matched-condition dependency. The older `mechanistic_probe_steering_v1` interpretation is supporting/legacy. `action_history_disambiguation_v1` is a supporting diagnostic. The claim-bearing causal search is `causal_mech_v1`; `causal_specificity_v2` supplies the canonical aggregate metric and specificity interpretation.

The primary endpoint is `cognitive_counterfactual_recovery` with `global_cfr_v1`. The selected Qwen controller is layer 28, rank 2, basis SHA-256 `ef8776641159fe67d02fdd1eae16d342305c4118ccb640114e9eb62cf4fedab0`. Level-5B is `not_run`; target-preserving shuffled controls are `uninformative_control`.

Replay C02/C03 or inspect the stage with `python -m cognitive_discovery.reproduce stage causal_specificity_v2`. Do not interpret probe accuracy, steering, or old per-example CFR as the canonical mechanistic claim.
