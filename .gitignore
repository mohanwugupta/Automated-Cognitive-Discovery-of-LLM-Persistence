.DS_Store
.pytest_cache/
__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/

# Mechanistic scratch data must never enter the repository.
temporary_activations/
temporary_batches/
model_cache/
logs/

# Public Gate-B inputs are reproducibly downloaded and hash-verified. Keep the
# raw copies out of Git; commit only their immutable manifest and acquisition code.
validation/external_benchmark/raw/
